name: component_library_storybook
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  component_library:
    path: ../
  storybook_flutter: ^0.14.0
  cached_network_image: ^3.3.0
  provider: ^6.0.5
  suivi_de_module:
    path: ../../../
  eleve_repository:
    path: ../../eleve_repository

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
